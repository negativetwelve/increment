{
  "name": "@increment/theme",
  "version": "1.5.0",
  "main": "build/index.js",
  "dependencies": {
    "@increment/core": "1.5.0"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
