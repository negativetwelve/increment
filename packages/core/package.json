{
  "name": "@increment/core",
  "version": "1.6.0",
  "main": "build/index.js",
  "dependencies": {
    "@increment/babel": "1.6.0"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
