{
  "name": "@increment/core",
  "version": "0.34.0",
  "main": "build/index.js",
  "dependencies": {
    "@increment/babel": "0.34.0"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
