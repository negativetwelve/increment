{
  "name": "@increment/core",
  "version": "2.0.0",
  "main": "build/index.js",
  "dependencies": {
    "@increment/babel": "2.0.0"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
