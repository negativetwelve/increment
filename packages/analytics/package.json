{
  "name": "@increment/analytics",
  "version": "0.8.3",
  "main": "build/index.js",
  "dependencies": {
    "@increment/components": "0.8.3",
    "prop-types": "15.6.0",
    "react": "16.4.2"
  },
  "babel": {
    "presets": [
      "module:@increment/babel"
    ]
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
