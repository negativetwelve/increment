{
  "name": "@increment/analytics",
  "version": "0.33.0",
  "main": "build/index.js",
  "dependencies": {
    "@increment/components": "0.33.0",
    "@increment/core": "0.33.0",
    "prop-types": "15.7.2",
    "react": "16.8.3"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
