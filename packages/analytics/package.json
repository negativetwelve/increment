{
  "name": "@increment/analytics",
  "version": "0.15.0",
  "main": "build/index.js",
  "dependencies": {
    "@increment/components": "0.15.0",
    "prop-types": "15.6.0",
    "react": "16.4.2"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
