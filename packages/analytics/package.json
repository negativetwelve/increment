{
  "name": "@increment/analytics",
  "version": "0.7.1",
  "main": "build/index.js",
  "dependencies": {
    "@increment/components": "0.7.1",
    "prop-types": "15.6.0",
    "react": "16.4.1"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
