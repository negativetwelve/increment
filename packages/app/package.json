{
  "name": "@increment/app",
  "version": "1.2.0",
  "main": "build/index.js",
  "dependencies": {
    "@increment/core": "1.2.0",
    "@increment/graphql": "1.2.0",
    "apollo-client": "2.6.4",
    "cross-fetch": "2.1.0",
    "graphql": "14.2.1",
    "prop-types": "15.7.2",
    "react": "16.8.3",
    "react-router-dom": "5.0.0"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
