{
  "name": "@increment/app",
  "version": "0.8.7",
  "main": "build/index.js",
  "dependencies": {
    "@increment/graphql": "0.8.7",
    "apollo-client": "2.5.1",
    "cross-fetch": "2.1.0",
    "graphql": "14.2.1",
    "react": "16.4.2",
    "react-apollo": "2.5.5",
    "react-router-dom": "5.0.0"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
