{
  "name": "@increment/app",
  "version": "0.8.4",
  "main": "build/index.js",
  "dependencies": {
    "@increment/graphql": "0.8.4",
    "cross-fetch": "2.1.0",
    "react": "16.4.2",
    "react-apollo": "2.1.11",
    "react-router-dom": "4.2.2"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
