{
  "name": "@increment/theme-basic",
  "version": "1.1.1",
  "main": "build/index",
  "dependencies": {
    "@increment/theme": "1.1.1"
  },
  "scripts": {
    "build": "babel src --out-dir build --ignore \"**/__tests__/**\"",
    "clean": "rimraf build",
    "prepublishOnly": "yarn run clean && yarn run build"
  }
}
